# bridge/.env.example — WhatsApp Bridge (Fresh Start)
# Copy to bridge/.env — NEVER commit real secrets.
# New WhatsApp manager session + new group. See START.md.

BRIDGE_API_KEY=
MONGODB_URI=
GAS_SCRIPT_URL=
CORS_ORIGINS=http://localhost:5273,http://localhost:5275
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
ALERT_EMAIL=
RENDER_URL=
PORT=3000
