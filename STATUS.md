# Status

## מצב נוכחי
- גל: **Wave Unify** — 2 מסכים + תשתית חדשה + SETUP.bat
- כניסות: (1) מרכז שליטה `admin.html` (2) אפליקציית נסיעה `app.html` (בחירת תפקיד)

## עדכון אחרון
- **תיעוד:** README/GUIDE/MICROCOPY ל-2 מסכים
- **DevOps:** `SETUP.bat`, `.env.example` + `bridge/.env.example` לפרויקט חדש, `dev:ops`/`dev:app`/`build:unify`, מצב Vite `app`, stub `app.html` + `src/entries/app.tsx` (role picker). בלי secrets.
- **עיצוב / מתכנת:** ממשיכים IA + איחוד routing מלא

## הצעד הבא
1. @סוכן מתכנת — השלמת ops מאוחד + routing סופי ב-`app.html` (הפניות מהישנים)
2. @סוכן עיצוב — IA/טוקנים ל-2 אפליקציות
3. @סוכן תיעוד — סנכרון שמות פקודות סופיים (`dev:ops` / `dev:app` / `SETUP.bat`)
4. @סוכן בדיקות — smoke על 2 הכניסות אחרי מסירה
5. @noma — Firebase/GAS/WhatsApp חדשים ב-`.env` (מקומי בלבד)
