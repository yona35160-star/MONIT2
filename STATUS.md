# Status

## מצב נוכחי
- פרויקט: **TAXIPRO / MONIT2** — Wave Unify
- 2 כניסות: מרכז שליטה (`admin.html` / `dev:ops`) · אפליקציית נסיעה (`app.html` / `dev:app` + בחירת תפקיד)
- הפניות: `passenger.html` / `driver.html` / `index.html` → `app.html`

## עדכון אחרון
- **מתכנת Unify:** routing מעמיק — RoleShell + החלף תפקיד; הפניות ישנות; aliases `/station` `/dispatch`; `build:all`=`build:unify`
- **DevOps/תיעוד:** SETUP.bat + מדריכים (קודם)

## הצעד הבא
1. @סוכן בדיקות — smoke על 2 הכניסות בלבד + QA-REPORT
2. @סוכן תיעוד — לוודא שמות פקודות (`dev:ops` / `dev:app` / `SETUP.bat` / `build:unify`)
