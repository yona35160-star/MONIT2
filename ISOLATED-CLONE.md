# עותק מבודד — טקסי-פרו

זו העתקה מלאה של הקוד בלבד. היא **לא** מחוברת לפרויקט החי.

## מה נותק בכוונה

- Firebase המקורי — כל המפתחות אופסו, האפליקציה לא מאתחלת אותו
- Google Apps Script החי — אין URL, `clasp` לא מצביע על הסקריפט המקורי
- קבוצת הווטסאפ המקורית — `VITE_WHATSAPP_GROUP_JID` ריק
- סשן הווטסאפ המקורי — תיקיית `bridge/sessions` לא הועתקה
- מנהרת Cloudflare — לא מופעלת מכאן, כדי שלא תירשם ב-GAS המקורי
- פורטים שונים כדי לרוץ במקביל למקור: 5273 / 5274 / 5275 / גשר 3012

## לפני שימוש אמיתי

1. צור פרויקט Firebase חדש והדבק ב-`.env`
2. העתק את תיקיית `GS/` ל-Apps Script **חדש**, עדכן `GS/.clasp.json`
3. צור קבוצת ווטסאפ **חדשה** ושים את ה-JID ב-`.env.local`
4. הרץ `START-ALL.bat` וסרוק QR במספר ווטסאפ **חדש** (לא של התחנה הקיימת)

אל תריץ `clasp push` ואל תמלא את כתובת ה-GAS הישנה.
