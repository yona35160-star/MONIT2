# MICROCOPY — TAXIPRO / MONIT2

קווים מנחים לטקסטים באפליקציה (Passenger / Driver / Admin).

## קול מותג
- עברית ברורה, קצרה, מכבדת
- פנייה ברבים בפעולות/שגיאות: **נסו שוב**, **ודאו**, **הזינו**
- בלי Lorem / אנגלית מיותרת במסכי משתמש (חוץ משמות מוצר כמו TAXIPRO / WhatsApp)
- כפתורי CTA: פועל + תוצאה ("הזמן מונית עכשיו", "אמת והמשך")

## מצבי מערכת (סטנדרט)
| מצב | טקסט מומלץ |
|-----|------------|
| טעינה | `טוען...` / `מחשב נתונים...` / `שולח...` |
| שגיאת תקשורת | `שגיאת תקשורת. נסו שוב מאוחר יותר.` |
| שגיאת שליחה | `אירעה שגיאה בשליחת ההזמנה. נסו שוב.` |
| ריק / לא נמצא | `הזמנה לא נמצאה` |
| חיפוש נהג | `מאתר נהגים באזורך...` |

## מסלול קריטי — מילון קצר
| שלב | נוסע | נהג | אדמין |
|-----|------|-----|--------|
| הזמנה | הזמן מונית עכשיו | — | הזמנה ידנית / סטטוס |
| שיוך | מאתר נהגים באזורך... | נסיעה חדשה | שיוך נהג |
| מעקב | המונית שלי בדרך! | בדרך / הגיע / בנסיעה | מפת נהגים חיה |

## תיקוני Wave 1 (תיעוד)
- CustomerOrder: אחידות "נסו שוב" / "ודאו"
- Login (Admin): `Smart Central` → `מרכז שליטה`; `Authorized Personnel Only` → `למורשים בלבד`

## ייבוא רכיבי UI (מסירת עיצוב)
```tsx
import { Button, Badge, Card, Input, Spinner } from '../components/ui';
```

## תיקוני QA (Wave 1/2)
- QA-1: Admin Login — `מרכז שליטה` / `למורשים בלבד` (מקור מעודכן; נדרש rebuild לבדיקה)
- QA-6: `Smart Dispatch Hub` → `מרכז שיגור חכם` (CreateOrderModal / station-order)
- Login: `כתובת תשתית (Endpoint)` → `כתובת שרת`

## תיקוני Wave 2 המשך (מקומי)
- SystemStatusIndicator: מערכת מחוברת / מנותקת / מתחבר...
- DriversTable: מחובר / מנותק
- ServerStatusWidget: השרת לא זמין
- ErrorBoundary: שגיאה לא ידועה
- CreateOrderModal / OrderDetailsModal: חישוב מחיר נכשל
- api.ts: הודעות גשר/הרשאה בעברית

## Wave Unify — שני מסכים
| מסך | שם למשתמש | הערות |
|-----|-----------|--------|
| Ops | מרכז שליטה | כולל שיגור מתחנה — בלי «תחנה» כאתר נפרד |
| Travel | אפליקציית נסיעה | בחירת תפקיד: נוסע / נהג |
| Role gate | אני נוסע / אני נהג | קצר, ברור, בלי אנגלית |
| CTA נוסע | הזמן מונית עכשיו | נשאר |
| CTA נהג | התחל לקבל נסיעות | לפי מסך כניסה |

## Fresh Start v2 — מותג ויזואלי (העתק טקסט)
- כיוון: **מוניות ענבר** על **פחם כהה** (ראה `design-system.json` v2 + `DESIGN_NOTES.md`)
- פונט מוביל ב-UI: **Heebo**
- CTA ראשי: קצר וחד — עדיין «הזמן מונית עכשיו» / «אמת והמשך» / «אני נוסע» / «אני נהג»
- אל תחזירו ניסוחי מותג ישנים של «sky/indigo» במסמכים או במסכים
